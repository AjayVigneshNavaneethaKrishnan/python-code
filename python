n=input("Number of Applications:")
if n.isdigit() == True:
    n=int(n)
    list_app=[]
    for i in range(n):
        print("Enter the application detail"+str(i+1))
        an = input("Enter Application name:")
        au = input("Enter Application author name:")
        vr = input("Enter Application version:")
        yr = input("Enter Application year:")
        pr = input("Enter Application price:")
        list_app.append([an,au,vr,yr,pr])
    aut = input("Enter the author name to check:")
    for i in range(n):
        if(list_app[i][1]) == aut:
            print(list_app[i])    
price = input("Enter the Application price")
for i in range(n):    
    print(list_app[i])    
#Sort the price in ascending order    
for i in range(n):    
    for j in range(i+1):    
        if(pr[i] > prr[j]):    
            temp = pr[i];    
            pr[i] = pr[j];    
            pr[j] = temp;    
            print();    
     
#Displaying price of application after sorted    
    
print(" price of application sorted in ascending order: ");  
for i in range(n):    
    print(pr[i]) 
    year = input("Enter the year to check:")
    if yr.isdigit() == True:
            yr=int(yr)
    else:
            print("Enter Valid Year")
            exit()
            pr = int(input("Enter Application price:"))
            list_app.append([an,au,vr,yr,pr])
            print(list_app[i])
else:
    print("Enter valid number for Number of Application")

    
